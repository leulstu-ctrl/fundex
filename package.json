{
  "name": "fundednext-main-account",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "playwright": "^1.58.1"
  }
}
